# Web framework
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9

# LLM
anthropic>=0.34,<1.0

# Document parsing
pymupdf>=1.24,<2.0
pdf2image>=1.17,<2.0
Pillow>=10.3,<12.0
lxml>=5.2,<6.0
python-docx>=1.1,<2.0

# OCR (default: Tesseract via pytesseract)
pytesseract>=0.3.10

# File type detection
python-magic>=0.4.27; sys_platform != "win32"
python-magic-bin>=0.4.14; sys_platform == "win32"

# Validation / config
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# Auth
python-jose[cryptography]>=3.3,<4.0

# HTTP client (reexports used by tests and Anthropic SDK)
httpx>=0.27,<1.0

# Async processing (optional — only needed when ENABLE_CELERY=true)
celery[redis]>=5.4,<6.0
redis>=5.0,<6.0

# Structured logging
structlog>=24.1
